[package]
name = "counter"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasmlette-contracts-sdk = { path = "../.." }

[profile.release]
opt-level = "z"
lto = true
