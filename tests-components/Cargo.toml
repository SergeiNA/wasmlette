[package]
name = "wasmlette-tests-component"
version.workspace = true
edition.workspace = true

[dependencies]
wasmlette-blockchain = { path = "../crates/blockchain" }
wasmlette-runtime = { path = "../crates/runtime" }
wasmlette-node = { path = "../crates/node" }
tracing = "0.1"
tracing-subscriber = { version = "0.3", default-features = false, features = ["fmt", "env-filter"] }

[lib]
# This is not a library, just a test crate
path = "lib.rs"

[[test]]
name = "tests-component"
path = "tests/component_tests.rs"
